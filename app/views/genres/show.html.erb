<%= @genre.name %>
<% @genre.songs.each do |song| %>
<%= song.title %>
<%= song.length %>
<%= song.play_count %>
<% end %>
Average Song Rating: <%= @genre.average_rating unless @genre.songs.empty? %>
